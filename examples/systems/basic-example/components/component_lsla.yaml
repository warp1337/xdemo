-  xdemocomponent:
    platform: "posix"
    command: "ls -la $XDEMOROOT; ls -la $XDEMOPREFIX"
    autostart: true
    observer:
      - stdout:
          maxwaittime: 0.5
          criteria: "environment-posix.sh"
    errorpolicy: "strict"
    description: "In computing, ls is a command to list files in Unix and Unix-like operating systems."